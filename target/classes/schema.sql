CREATE SCHEMA IF NOT EXISTS dbo;

CREATE TABLE IF NOT EXISTS dbo.test_ITEMMST (
    PLANT VARCHAR(50),
    ITEM VARCHAR(100),
    ITEMDESC VARCHAR(255),
    UnitPrice DECIMAL(10,2),
    COST DECIMAL(10,2),
    STKUOM VARCHAR(20),
    PRD_CLS_ID VARCHAR(50),
    USERFLD1 VARCHAR(100),
    ARTIST VARCHAR(100),
    TITLE VARCHAR(255)
);

CREATE TABLE IF NOT EXISTS dbo.test_RECVDET (
    PLANT VARCHAR(50),
    ITEM VARCHAR(100),
    LOC VARCHAR(100),
    QTY INT,
    COST DECIMAL(10,2),
    UPAT VARCHAR(50)
);

CREATE TABLE IF NOT EXISTS dbo.test_INVMST (
    PLANT VARCHAR(50),
    ITEM VARCHAR(100),
    LOC VARCHAR(100),
    QTY INT,
    WHID VARCHAR(100)
);
